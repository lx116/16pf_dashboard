// Divider.js
import React from 'react';

const Divider = () => {
    return (
        <div
            style={{
                borderBottom: '2px solid black', // Línea sólida con espesor de 2px
                margin: '10px 0', // Márgenes arriba y abajo de 10px
                width: '100%' // Ancho al 100% del contenedor
            }}
        />
    );
};

export default Divider;
